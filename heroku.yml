build:
  docker:
    web: Dockerfile
run:
  web: python app.py
 
# For deployment on platforms like Heroku/Koyeb
# Add these buildpacks if needed:
# https://github.com/heroku/heroku-buildpack-activestorage-preview
# https://github.com/heroku/heroku-buildpack-apt
